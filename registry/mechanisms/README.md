# Mechanisms

Each `*.mechanism.json` file conforms to [`mechanism.schema.json`](../schemas/mechanism.schema.json). A Mechanism is the stable connection point between lessons, Agent implementations, Claims, Experiments, and website navigation. A published ID must never be reused for a different meaning.
