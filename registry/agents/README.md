# Agent Profiles

Each `*.agent.json` file conforms to [`agent-profile.schema.json`](../schemas/agent-profile.schema.json). A profile describes stable identity, source availability, and surfaces; changing architecture facts belong to immutable snapshots and Claims.
