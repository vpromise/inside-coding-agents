# Claims

Each `*.claim.json` file conforms to [`claim.schema.json`](../schemas/claim.schema.json). A Claim states one snapshot-bound fact and labels its Evidence as `source`, `official-doc`, `reproduced`, or `inference`.
